# Test Manifest

The suite contains 60+ unit tests across sales, labor, profit/commission, tax rules, customer buy-back, exchange, melted gold, purity/price conversion, reverse solving, validation/edge cases, and golden snapshots.

The Dart test suite is in `test/gold_calculation_engine_test.dart`. A dependency-free assertion runner is in `bin/self_test.dart`.
