name: gold_calculator_app
publish_to: 'none'
description: Offline-first Persian gold shop calculator MVP.
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  gold_calculator_engine:
    path: packages/gold_calculator_engine
  supabase_flutter: ^2.0.0

flutter:
  uses-material-design: true
